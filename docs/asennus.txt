Services kannan asennusohje

Vaadittavat ohjelmat:

- postgresql-server >= 9.0
- python >= 2.6 ja < 3.0
 - pythonin riippuvuudet löytyy requirements.txt:stä

cd sql
export HOSTMASTER_ADDRESS='hostmaster@mydomain.dom'
export POSTGRES_USER='postgres'
export POSTGRES_SERVER='localhost'
./create_database.sh

- Lisää services-kansio PYTHONPATH:iin
- Asentele renkisrv ja renki tai/ja renkiadm 